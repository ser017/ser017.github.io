{
  "name": "my-articles-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf _site || true"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0"
  }
}